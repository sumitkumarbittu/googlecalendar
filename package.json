{
  "name": "calendar-transcription-api",
  "version": "1.0.0",
  "description": "Google Calendar Read/Write API with transcription",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "docker:build": "docker build -t calendar-api .",
    "docker:run": "docker run -p 3001:3001 --env-file .env calendar-api",
    "test": "curl http://localhost:3001/api/health"
  },
  "dependencies": {
    "express": "^4.18.2",
    "googleapis": "^128.0.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "cookie-parser": "^1.4.6",
    "express-session": "^1.17.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}